import{X as e,Z as t,t as n,ut as r}from"./chunks/framework.es-12sf8.js";var i=JSON.parse(`{"title":"Settings","description":"","frontmatter":{},"headers":[],"relativePath":"usage/settings.md","filePath":"en/usage/settings.md","lastUpdated":1788278373000}`),a={name:`usage/settings.md`};function o(n,i,a,o,s,c){return r(),e(`div`,null,[...i[0]||=[t(`<h1 id="settings" tabindex="-1">Settings <a class="header-anchor" href="#settings" aria-label="Permalink to “Settings”">​</a></h1><p>Open <strong>Settings &gt; Sync Engine</strong> to change these options.</p><p>Some settings are registered by modules, their usage are documented in module specific pages.</p><h2 id="core" tabindex="-1">Core <a class="header-anchor" href="#core" aria-label="Permalink to “Core”">​</a></h2><h3 id="storage-backend" tabindex="-1">Storage Backend <a class="header-anchor" href="#storage-backend" aria-label="Permalink to “Storage Backend”">​</a></h3><p>Choose the installed module that connects Sync Engine to your storage service. The connection icon beside the selector checks whether the selected service can be reached, automatically refreshed every 10 seconds, you can also manually trigger a connectivity check by clicking it. Configure the selected service in its module settings when needed. See the <a href="./modules">available modules</a> for supported backends.</p><h3 id="module-management" tabindex="-1">Module Management <a class="header-anchor" href="#module-management" aria-label="Permalink to “Module Management”">​</a></h3><p>Open the module management page. From there, you can install modules from sources or local files, and update, enable, disable, remove, or edit modules. Modules provide storage backends and extra sync strategies. Review <a href="./security">Security</a> before installing modules from sources you do not control.</p><p>The <a href="./../deep-dive/module-management-page">module management page</a> contains a search bar, an installed-only filter, an install-from-file entry, and module cards. Module source URLs are edited in the <strong>Module sources</strong> page under Development.</p><h3 id="auto-update-modules" tabindex="-1">Auto-Update Modules <a class="header-anchor" href="#auto-update-modules" aria-label="Permalink to “Auto-Update Modules”">​</a></h3><p>Automatically update installed modules from their configured sources. Enabled by default. Turn this off when you want to review updates manually.</p><h3 id="sync-strategy" tabindex="-1">Sync Strategy <a class="header-anchor" href="#sync-strategy" aria-label="Permalink to “Sync Strategy”">​</a></h3><p>Choose how Sync Engine decides what to do when local and remote files differ:</p><ul><li><strong>Bidirectional</strong> is the default. It can apply changes on both sides and asks the selected conflict resolver to handle simultaneous file changes.</li><li><strong>Mirror local</strong> makes local vault authoritative. It copies local entries to remote and removes remote-only entries.</li><li><strong>Mirror remote</strong> makes remote storage authoritative. It copies remote entries to local and removes local-only entries.</li></ul><p>Mirror strategies overwrite changes on the non-authoritative side without conflict resolution. Other strategies may be supplied by modules.</p><h3 id="conflict-resolve-strategy" tabindex="-1">Conflict Resolve Strategy <a class="header-anchor" href="#conflict-resolve-strategy" aria-label="Permalink to “Conflict Resolve Strategy”">​</a></h3><p>Choose what happens when the same file changed locally and remotely since the last successful sync. Available choices include:</p><ul><li><strong>Rename and keep both</strong>: keep both versions by renaming the older one to <code>&lt;original name&gt;.conflict.&lt;extension&gt;</code>. This is the default and avoids silently losing either version.</li><li><strong>Latest survives</strong>: keep version with the newer modification time.</li><li><strong>Keep local</strong>: replace remote version with local version.</li><li><strong>Keep remote</strong>: replace local version with remote version.</li><li><strong>Skip</strong>: leave conflict unchanged until you resolve it another way.</li></ul><p>Use a strategy that matches how you work. The replace and latest options can discard changes when timestamps are wrong or clocks differ between devices. The optional <a href="./../deep-dive/modules/smart-merge">Smart Merge module</a> adds a text-merge strategy for supported files.</p><h2 id="features" tabindex="-1">Features <a class="header-anchor" href="#features" aria-label="Permalink to “Features”">​</a></h2><h3 id="realtime-sync" tabindex="-1">Realtime Sync <a class="header-anchor" href="#realtime-sync" aria-label="Permalink to “Realtime Sync”">​</a></h3><p>Automatically start a sync after files change. Enter the delay between the last file change and the sync, for example <code>500ms</code> or <code>5s</code>. If you edit several files quickly, Sync Engine waits until the delay passes instead of starting a separate sync for every edit.</p><p>Disabled by default. The default stored delay is <code>5s</code>.</p><h3 id="startup-sync" tabindex="-1">Startup Sync <a class="header-anchor" href="#startup-sync" aria-label="Permalink to “Startup Sync”">​</a></h3><p>Start one sync automatically after the plugin loads. Enter how long to wait after startup, for example <code>5s</code> or <code>1min</code>.</p><p>Disabled by default. The default stored delay is <code>5s</code>.</p><h3 id="scheduled-sync" tabindex="-1">Scheduled Sync <a class="header-anchor" href="#scheduled-sync" aria-label="Permalink to “Scheduled Sync”">​</a></h3><p>Start syncs repeatedly at a fixed interval. Enter an interval such as <code>10min</code> or <code>0.5h</code>. This is useful when you want regular syncs without reacting to every edit.</p><p>Disabled by default. The default interval is <code>15min</code>. The interval must be greater than zero.</p><h3 id="realtime-sync-fast-mode" tabindex="-1">Realtime Sync Fast Mode <a class="header-anchor" href="#realtime-sync-fast-mode" aria-label="Permalink to “Realtime Sync Fast Mode”">​</a></h3><p>Reuse recently cached remote information during realtime syncs and skip some remote discovery work. This can make quick edits sync faster, especially for large remote folders.</p><p>Enabled by default. Turn it off when realtime sync must always perform a full remote check, such as when files are often changed outside Obsidian.</p><h3 id="asymmetric-storage" tabindex="-1">Asymmetric Storage <a class="header-anchor" href="#asymmetric-storage" aria-label="Permalink to “Asymmetric Storage”">​</a></h3><p>Store remote files in a flat layout with generated names instead of mirroring your vault&#39;s folders. Sync Engine uses its records to restore the original local paths. This can substantially speed up syncs and reduce work in storage services that handle large folder trees poorly.</p><p>This changes how the remote storage looks. Read the separate <a href="./../deep-dive/asymmetric-storage">Asymmetric Storage guide</a> for its design and limitations before enabling it:</p><ul><li>Remote files will not be readable in their normal folder structure.</li><li>Every device using the vault must use the same setting.</li><li>Changing this setting for an existing vault opens a migration prompt. Cancel leaves the current setting unchanged.</li><li><strong>Toggle without migration</strong> changes the setting without moving remote files or clearing records. Use it only when the remote storage already has the target layout, such as after migrating on another device.</li><li><strong>Start migration</strong> updates local state, clears matching records, removes known remote entries, and repopulates the remote storage with the new layout. Do not start it concurrently on multiple devices.</li><li>Enable it only when you do not need to browse the remote files as ordinary files.</li></ul><p>Enabled by default.</p><h2 id="controls" tabindex="-1">Controls <a class="header-anchor" href="#controls" aria-label="Permalink to “Controls”">​</a></h2><p>These options limit sync resource use. They are useful for storage services with request limits or devices with limited memory.</p><h3 id="max-file-size" tabindex="-1">Max File Size <a class="header-anchor" href="#max-file-size" aria-label="Permalink to “Max File Size”">​</a></h3><p>Skip files larger than the entered limit. This does not change or delete the file; it is left out of sync operations.</p><p>Disabled by default. The default stored limit is <code>30MB</code>.</p><h3 id="max-request-concurrency" tabindex="-1">Max Request Concurrency <a class="header-anchor" href="#max-request-concurrency" aria-label="Permalink to “Max Request Concurrency”">​</a></h3><p>Limit how many remote requests can run at the same time. A lower value can help services that reject too many simultaneous requests. A higher value can improve speed when the service and network allow it.</p><p>Enabled by default with a limit of <code>50</code> requests.</p><h3 id="min-request-interval" tabindex="-1">Min Request Interval <a class="header-anchor" href="#min-request-interval" aria-label="Permalink to “Min Request Interval”">​</a></h3><p>Require a minimum wait between remote requests. Use this when a service enforces a request-per-second limit. This works alongside <strong>Max request concurrency</strong>: one limits simultaneous work, while this one spaces requests out.</p><p>Disabled by default. The default stored interval is <code>0</code>.</p><h3 id="max-memory-consumption" tabindex="-1">Max Memory Consumption <a class="header-anchor" href="#max-memory-consumption" aria-label="Permalink to “Max Memory Consumption”">​</a></h3><p>Limit memory used while Sync Engine processes files. A lower limit can help on phones or other memory-constrained devices, but may reduce sync throughput.</p><p>Enabled by default with a limit of <code>100MB</code>.</p><h3 id="value-formats" tabindex="-1">Value Formats <a class="header-anchor" href="#value-formats" aria-label="Permalink to “Value Formats”">​</a></h3><ul><li>File sizes accept <code>B</code>, <code>KB</code>, <code>MB</code>, <code>GB</code>, or <code>TB</code>, for example <code>10MB</code> or <code>0.5GB</code>.</li><li>Time values accept <code>ms</code>, <code>s</code>, <code>min</code>, <code>h</code>, or <code>d</code>, for example <code>500ms</code>, <code>5s</code>, or <code>1h</code>.</li><li>Numeric values such as request concurrency accept a number, for example <code>50</code>.</li><li>Values must be zero or greater. Settings that represent a limit or interval may reject zero. Invalid values remain marked with a warning and are not saved until corrected.</li></ul><h2 id="inclusion-and-exclusion-rules" tabindex="-1">Inclusion and Exclusion Rules <a class="header-anchor" href="#inclusion-and-exclusion-rules" aria-label="Permalink to “Inclusion and Exclusion Rules”">​</a></h2><p>Use glob rules to control which files and folders Sync Engine synchronizes.</p><ul><li><strong>Exclusion rules</strong> stop matching files and folders from syncing.</li><li><strong>Inclusion rules</strong> make exceptions to exclusion rules.</li><li>Files and folders matching neither list sync normally.</li></ul><p>Sync Engine evaluates each file and folder against both lists:</p><ol><li>A matching inclusion rule takes precedence, even when an exclusion rule also matches.</li><li>An item inside an excluded folder remains excluded unless an inclusion rule matches that item or a descendant that should be kept.</li><li>An item matching only an exclusion rule does not sync.</li><li>An item matching neither list syncs.</li></ol><p>Example:</p><div class="language-text"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>Exclusion rule: private/</span></span>
<span class="line"><span>Inclusion rule: private/keep.md</span></span></code></pre></div><p><code>private/keep.md</code> syncs; other files under <code>private/</code> remain excluded. Add inclusion rules for files or subtrees that should pass through excluded folders.</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>If you sync Obsidian&#39;s plugin directory, you need to <strong>exclude Sync Engine&#39;s modules folder in any situation (always ensure <code>.obsidian/plugins/sync-engine/modules</code> exists in your exclusion rules)</strong>. <a href="./../deep-dive/extensibility">Due to security considerations</a>, all untracked modifications in that folder (including sync runs) will trigger Sync Engine&#39;s security protection mechanism. And will cause the modules fail to load.</p></div><h3 id="writing-rules" tabindex="-1">Writing Rules <a class="header-anchor" href="#writing-rules" aria-label="Permalink to “Writing Rules”">​</a></h3><p>Rules use <a href="https://en.wikipedia.org/wiki/Glob_(programming)" target="_blank" rel="noreferrer"><code>glob</code> expressions</a>:</p><table tabindex="0"><thead><tr><th>Rule</th><th>Matches</th></tr></thead><tbody><tr><td><code>*.log</code></td><td>Log files anywhere in your vault</td></tr><tr><td><code>temp/</code></td><td>Folders named <code>temp</code> and their contents</td></tr><tr><td><code>notes/**/*.md</code></td><td>Markdown files inside <code>notes</code> and its subfolders</td></tr><tr><td><code>test-files/**/*</code></td><td>Everything inside <code>test-files</code> and its subfolders</td></tr><tr><td><code>**/.trash/</code></td><td>Any folder named <code>.trash</code></td></tr><tr><td><code>/.obsidian/</code></td><td><code>.obsidian</code> at vault root</td></tr></tbody></table><p><code>*</code> matches within one path segment. <code>**</code> also crosses subfolders. <code>?</code> matches one character. Add <code>/</code> at the end to match folders. Rules are case-insensitive by default; use the case-sensitive button in the rule editor when needed. Start a rule with <code>/</code> to anchor it at the vault root.</p><div class="tip custom-block"><p class="custom-block-title custom-block-title-default">TIP</p><p>Sync Engine avoids walking excluded subtrees. When an excluded folder cannot contain anything matched by an inclusion rule, traversal stops at that folder. If an inclusion rule could match a descendant, Sync Engine probes that folder to find the included content.</p><p>Anchor inclusion rules when their location is known. For example, use <code>/projects/current/**/*</code> instead of <code>projects/current/**/*</code> when <code>projects</code> is at the vault root. An anchored rule narrows the possible path immediately, reducing probes through unrelated excluded folders. Unanchored rules can match the same path shape at any depth, so they may require more traversal.</p></div><h2 id="miscellaneous" tabindex="-1">Miscellaneous <a class="header-anchor" href="#miscellaneous" aria-label="Permalink to “Miscellaneous”">​</a></h2><h3 id="custom-headers" tabindex="-1">Custom Headers <a class="header-anchor" href="#custom-headers" aria-label="Permalink to “Custom Headers”">​</a></h3><p>Add HTTP headers to every request sent to the remote storage service. Some services use headers for a tenant name, API version, or other account-specific option.</p><p>Use a plaintext header for a non-sensitive value. Use a secret header for a token or other private value; secret values are stored in Obsidian&#39;s keychain instead of in the normal plugin settings. See <a href="./permissions#obsidian-secret-storage">Runtime Permissions</a> for when Sync Engine reads these secrets. Header names must be valid and match what your storage service expects. Empty header rows are ignored when saved.</p><p>Do not add credentials as plaintext headers when a secret header is available. Incorrect headers can make connection checks and syncs fail. Backend modules may have their own credential settings; follow the module&#39;s documentation for those.</p><h3 id="notice-sync-status-on-mobile" tabindex="-1">Notice Sync Status on Mobile <a class="header-anchor" href="#notice-sync-status-on-mobile" aria-label="Permalink to “Notice Sync Status on Mobile”">​</a></h3><p>Show a notice on mobile while a sync is running. On desktop, the status bar shows this information instead.</p><p>Enabled by default.</p><h3 id="confirm-operations-in-manual-sync" tabindex="-1">Confirm Operations in Manual Sync <a class="header-anchor" href="#confirm-operations-in-manual-sync" aria-label="Permalink to “Confirm Operations in Manual Sync”">​</a></h3><p>Show planned file operations before a manually started sync runs. You can review and confirm them. This does not affect automatic syncs.</p><p>Enabled by default.</p><h3 id="confirm-deletions-during-auto-sync" tabindex="-1">Confirm Deletions During Auto-Sync <a class="header-anchor" href="#confirm-deletions-during-auto-sync" aria-label="Permalink to “Confirm Deletions During Auto-Sync”">​</a></h3><p>Ask before an automatic sync deletes local files. In the confirmation window, you can accept the deletions or choose to re-upload those files instead.</p><p>Enabled by default. This applies to realtime, startup, and scheduled syncs.</p><h2 id="development" tabindex="-1">Development <a class="header-anchor" href="#development" aria-label="Permalink to “Development”">​</a></h2><p>These actions are mainly for troubleshooting. Use them carefully.</p><h3 id="clear-records" tabindex="-1">Clear Records <a class="header-anchor" href="#clear-records" aria-label="Permalink to “Clear Records”">​</a></h3><p>Sync Engine keeps records of which local and remote files were last matched. It uses these records to tell a new change from a conflict and to plan safe sync operations.</p><p>Clicking <strong>Clear</strong> button removes records for the current backend + vault combo.</p><p>These actions do not directly delete files, but the next sync will have less history and may plan uploads, downloads, or deletions differently. The setting warns that clearing records can cause data loss. Only use it when you manually modified remote files and the plugin already reports wrong operations.</p><h3 id="export-logs-to-file" tabindex="-1">Export Logs to File <a class="header-anchor" href="#export-logs-to-file" aria-label="Permalink to “Export Logs to File”">​</a></h3><p>Write Sync Engine&#39;s logs to a file in <code>&lt;vault root&gt;/Sync Engine Logs/</code> folder. Use this when reporting a failed sync or investigating a connection problem. Logs can contain paths and service details, so review the file before sharing it.</p><h3 id="module-sources" tabindex="-1">Module Sources <a class="header-anchor" href="#module-sources" aria-label="Permalink to “Module Sources”">​</a></h3><p>Edit the URLs Sync Engine uses to obtain the module catalog. URLs must use <code>http:</code> or <code>https:</code> and invalid entries are marked until corrected; use HTTPS whenever possible.</p><p>The default source is <code>https://sync.consensia.cc/modules.json</code>. An alternative GitHub-hosted source <code>https://raw.githubusercontent.com/hesprs/sync-engine/refs/heads/gh-pages/modules-alternative.json</code> is available for networks that cannot reach the default source. Use one official source at a time to avoid duplicate module entries.</p>`,92)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};