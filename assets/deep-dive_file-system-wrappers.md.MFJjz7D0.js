import{X as e,Z as t,t as n,ut as r}from"./chunks/framework.es-12sf8.js";var i=JSON.parse(`{"title":"File System Wrappers","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/file-system-wrappers.md","filePath":"en/deep-dive/file-system-wrappers.md","lastUpdated":1788361686000}`),a={name:`deep-dive/file-system-wrappers.md`};function o(n,i,a,o,s,c){return r(),e(`div`,null,[...i[0]||=[t(`<h1 id="file-system-wrappers" tabindex="-1">File System Wrappers <a class="header-anchor" href="#file-system-wrappers" aria-label="Permalink to “File System Wrappers”">​</a></h1><p>A wrapper is a factory function around a <a href="./file-system">file system</a> <code>Fs</code> instance that intercepts the behavior of the original FS. A wrapper function receives the original FS in the first argument and returns an <code>Fs</code>. Overlay wrappers return a <code>WrappedFs</code> with an <code>original</code> member; injection wrappers modify the supplied FS and return it. Infinite layers of overlay wrappers can be applied to the same FS instance.</p><p>The SDK&#39;s canonical <code>prefixWrapper</code> API and usage example are documented in <a href="./../development/file-system#prefixwrapper">development: file system</a>.</p><p>The root FS without any wrappers is typed <code>RootFs</code>. <code>Fs</code> is the union of <code>RootFs</code> and <code>WrappedFs</code>.</p><p>There are two kinds of wrappers:</p><ul><li><strong>Injection wrapper</strong>: changes some methods of the supplied FS by directly re-assigning public members. Does not produce a new layer.</li><li><strong>Overlay wrapper</strong>: most common, applies a new layer of wrapper at the top of original FS.</li></ul><h2 id="memory-control-wrapper" tabindex="-1">Memory Control Wrapper <a class="header-anchor" href="#memory-control-wrapper" aria-label="Permalink to “Memory Control Wrapper”">​</a></h2><ul><li>Target: local and remote <code>Fs</code></li><li>Priority: <code>1000</code> for both</li></ul><p>Applied on both local and remote. The wrapper receives a shared <code>MemoryControlSharedState</code> containing <code>memoryConsumption</code>, <code>hangingOperations</code>, and <code>maxMemory</code>.</p><p><code>hangingOperations</code> pool is kept sorted in descending order by transfer size (<code>stat.size</code>), so longer transfers resume before shorter ones. This front-loads large files across the whole sync instead of deferring them into an underutilized tail.</p><p>Only intercept <code>read</code>, <code>readStream</code>, <code>write</code>, <code>writeStream</code> calls:</p><ol><li>When <code>read()</code> and <code>readStream()</code> arrive, reserve <code>min(stat.size, 16 MiB)</code>. If memory is unavailable, put the operation into the sorted pool and delay it. On failure, release the reservation and resume queued operations. When consumption is zero, one operation may exceed <code>maxMemory</code>.</li><li><code>write()</code> and <code>writeStream()</code> do not reserve memory. When either finishes or fails, release <code>min(stat.size, 16 MiB)</code>, then resume queued reads when memory allows.</li></ol><p>When resuming, the wrapper scans the whole pool and admits every operation that fits, skipping larger ones that do not. This backfilling keeps leftover budget packed with smaller transfers instead of stalling behind a single oversized operation.</p><h2 id="optimization-wrapper" tabindex="-1">Optimization Wrapper <a class="header-anchor" href="#optimization-wrapper" aria-label="Permalink to “Optimization Wrapper”">​</a></h2><ul><li>Target: local and remote <code>Fs</code></li><li>Priority: <code>2000</code> for both</li><li>Mechanism: <code>setTimeout(..., 0)</code>-scheduled atom queue</li></ul><h3 id="optimization-companion-wrapper" tabindex="-1">Optimization Companion Wrapper <a class="header-anchor" href="#optimization-companion-wrapper" aria-label="Permalink to “Optimization Companion Wrapper”">​</a></h3><ul><li>Target: local and remote <code>Fs</code></li><li>Priority: <code>21000</code> for both</li><li>Behavior: injects <code>read()</code> and <code>readStream()</code> methods that add keys to the opposite-side optimization pool and probe the opposite-side FS.</li></ul><h3 id="backend-dependent-optimization" tabindex="-1">Backend-Dependent Optimization <a class="header-anchor" href="#backend-dependent-optimization" aria-label="Permalink to “Backend-Dependent Optimization”">​</a></h3><p>Sync routines must remain backend-independent, but optimal execution strategies vary (e.g., WebDAV requires sequential parent directory creation; S3 allows concurrent uploads). This wrapper decouples logic from optimization by intercepting FS API calls at the root layer to reorder, batch, or schedule execution within promises.</p><p>Backends may extend <code>RootFs</code> with backend-specific methods. The batch optimizer can receive the FS via <code>instanceof</code> check and use those methods.</p><h3 id="operation-coalescing" tabindex="-1">Operation Coalescing <a class="header-anchor" href="#operation-coalescing" aria-label="Permalink to “Operation Coalescing”">​</a></h3><p>Coalescing uses a zero-delay timer: mutations are queued synchronously, and the timer flush captures operations added before it runs. The optimization wrapper keeps separate <code>Set&lt;string&gt;</code> pools for local and remote keys.</p><p><strong>Interception Rules</strong>:</p><ol><li>Mutations (<code>delete</code>, <code>mkdir</code>, <code>move</code>): Enqueued as <code>InputAtom</code>s.</li><li>Reads (<code>read</code>, <code>readStream</code>): The companion wrapper adds <code>stat.key</code> to the opposite-side <code>Set</code> and probes that FS with the same key and stat. The opposite-side optimization wrapper consumes a matching needle, records its transformed key, and terminates the probe.</li><li>Writes (<code>write</code>, <code>writeStream</code>): <ul><li>Reuses deferred execution if a pending anticipated write exists for the delegated key.</li><li>Passes through otherwise.</li></ul></li><li>Pass-through: ordinary <code>read</code> and <code>readStream</code> calls, plus <code>getUid</code>, <code>stat</code>, <code>exists</code>, and <code>list</code>, delegate to the original FS. Reads still intercept matching needle probes. <code>checkConnection</code> belongs to the remote backend entry, not <code>Fs</code>.</li></ol><p><strong>Execution</strong>:</p><p>On timer flush, the wrapper drains queued atoms and transformed keys into synthetic <code>write</code> atoms. Each anticipated write is stored in a key-based pending-write map; the later <code>write()</code> or <code>writeStream()</code> for that key supplies its real execution and reuses the deferred promise. These atoms are passed to the injected <code>batchOptimizer</code>. Single-atom queues execute directly without batching. Queued atoms share real execution and deferred promises via <code>createCachedPromise()</code>.</p><p>Every input atom has <code>resolve</code> and <code>reject</code> callbacks. Optimizers must propagate operation failures by rejecting the affected atoms, including when a custom atom replaces them or a batch returns per-key failures. Removed atoms must always be settled, or their original promises remain pending.</p><h2 id="asymmetric-storage-wrapper" tabindex="-1">Asymmetric Storage Wrapper <a class="header-anchor" href="#asymmetric-storage-wrapper" aria-label="Permalink to “Asymmetric Storage Wrapper”">​</a></h2><ul><li>Target: remote <code>Fs</code></li><li>Priority: <code>11000</code>, only when asymmetric storage is enabled</li></ul><p>Applied to the remote FS when asymmetric storage is enabled. It flattens hierarchical file and folder keys into anchored keys, then restores hierarchical stats and progress after delegation. Folders are represented by empty files. See the <a href="./asymmetric-storage">Asymmetric Storage specification</a>.</p><h2 id="context-wrapper" tabindex="-1">Context Wrapper <a class="header-anchor" href="#context-wrapper" aria-label="Permalink to “Context Wrapper”">​</a></h2><ul><li>Target: local and remote <code>Fs</code></li><li>Priority: <code>20000</code> for local; <code>10000</code> and <code>20000</code> for remote</li></ul><p>Intercepts <code>list()</code>, <code>stat()</code>, <code>write()</code>, <code>writeStream()</code>, <code>delete()</code>, <code>move()</code>, and <code>mkdir()</code> calls, and builds a copy of best-effort known stats in a <code>uni-kv</code> memory store that survives sync runs.</p><p><code>read()</code> and <code>readStream()</code> pass through unchanged. File stats are required by the FS interface.</p><p>Bootstrap uses these stores:</p><ul><li><code>localContext20000</code></li><li><code>remoteContext10000</code></li><li><code>remoteContext20000</code></li></ul><p>Behavior:</p><ul><li>On <code>stat()</code>, upsert the returned stat into the KV store</li><li>On <code>list()</code>, clear the store and reset according to the list result</li><li>On <code>write()</code> or <code>writeStream()</code>, upsert the supplied stat with the returned UID</li><li>On <code>delete()</code>, delete the record.</li><li>On <code>move()</code>, move the cached record to the new key and modify its <code>key</code> field.</li><li>On <code>mkdir()</code>, upsert folder record.</li><li>All memory database mutation should only happen when original operation succeeds and returns.</li><li>When the wrapper is created, compare its marker with the current FS UID. If they differ, clear the store and update the marker.</li></ul><h2 id="cancellation-wrapper" tabindex="-1">Cancellation Wrapper <a class="header-anchor" href="#cancellation-wrapper" aria-label="Permalink to “Cancellation Wrapper”">​</a></h2><ul><li>Target: local and remote <code>Fs</code></li><li>Priority: <code>3000</code> for both</li></ul><p>Receive <code>isCancelled: Ref&lt;boolean&gt;</code> in the second argument to detect sync cancellation.</p><p>Intercept all <code>Fs</code> methods except <code>getUid()</code>. Wrap all method calls with a throw if cancelled at before and after relaying. Special cases: only check cancellation <strong>before</strong> <code>read()</code> &amp; <code>readStream()</code> and <strong>after</strong> <code>write()</code> &amp; <code>writeStream()</code> to prevent cancellation race blocking memory control counter release.</p><h2 id="debug-wrapper" tabindex="-1">Debug Wrapper <a class="header-anchor" href="#debug-wrapper" aria-label="Permalink to “Debug Wrapper”">​</a></h2><p>The development-only <code>debugWrapper</code> overlays an FS and logs every method call. It is exported from the SDK development entrypoint and is not part of the runtime wrapper chain.</p><h3 id="prefix-wrapper" tabindex="-1">Prefix Wrapper <a class="header-anchor" href="#prefix-wrapper" aria-label="Permalink to “Prefix Wrapper”">​</a></h3><p><code>prefixWrapper</code> is an importable SDK wrapper for exposing a directory as the root of another <code>Fs</code>:</p><p>Its type is <code>prefixWrapper(original: Fs, prefix: string): WrappedFs</code>.</p><p>The wrapper normalizes <code>prefix</code> as a unified directory key: Unicode is normalized, percent-encoded path segments are decoded, empty slash segments are removed, and a trailing <code>/</code> is added. It prepends the normalized prefix when delegating file-system operations. Returned stat keys, list results, and list progress paths have the prefix removed; the wrapped directory itself is exposed as <code>/</code> and omitted from recursive list results. A returned path outside the prefix is rejected with an error.</p>`,48)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};