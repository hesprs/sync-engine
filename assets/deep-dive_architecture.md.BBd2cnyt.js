import{X as e,Z as t,t as n,ut as r}from"./chunks/framework.Cq50P25D.js";var i=JSON.parse(`{"title":"Sync Engine Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/architecture.md","filePath":"en/deep-dive/architecture.md","lastUpdated":1785588746000}`),a={name:`deep-dive/architecture.md`};function o(n,i,a,o,s,c){return r(),e(`div`,null,[...i[0]||=[t(`<h1 id="sync-engine-architecture" tabindex="-1">Sync Engine Architecture <a class="header-anchor" href="#sync-engine-architecture" aria-label="Permalink to “Sync Engine Architecture”">​</a></h1><p>Sync Engine is composed as an ordered set of internal modules. The plugin entrypoint supplies those modules to <a href="https://github.com/hesprs/synthkernel" target="_blank" rel="noreferrer">SynthKernel</a>, a small dependency-injection and reactivity runtime. The result is one typed context shared by the core, UI, and dynamically loaded modules.</p><p>The architecture has three boundaries:</p><ul><li><strong>Kernel boundary</strong>: SynthKernel constructs modules, injects their dependencies, and merges their public APIs into one context.</li><li><strong>Capability boundary</strong>: Registrar exposes typed factories and registries for file systems, requests, sync decisions, settings, and other extension points.</li><li><strong>Execution boundary</strong>: Sync operates on abstract local and remote file systems. Platform details enter through <code>VaultRequest</code>, <code>Request</code>, middleware, and file-system wrappers.</li></ul><p>The <a href="./sync">core sync routine</a>, <a href="./request">request abstraction</a>, and <a href="./file-system-wrappers">file-system wrappers</a> specify those execution boundaries in detail.</p><h2 id="runtime-lifecycle" tabindex="-1">Runtime Lifecycle <a class="header-anchor" href="#runtime-lifecycle" aria-label="Permalink to “Runtime Lifecycle”">​</a></h2><p><code>SyncEngine.onload()</code> performs the following sequence:</p><ol><li>Load settings.</li><li>Create a SynthKernel context for the internal module constructors.</li><li>Inject the loaded <code>settings</code> object and merge the declared context keys: <code>settings</code>, <code>root</code>, <code>events</code>, and <code>i18n</code>.</li><li>Load enabled external modules from the local module directory.</li><li>Add the plugin settings tab.</li><li>Call <code>start()</code> on every internal and loaded module in insertion order.</li></ol><p>On unload, modules with <code>dispose()</code> are called in reverse order. This gives later modules, such as UI and bootstrap modules, a chance to release resources before the services they use disappear.</p><p>The initial context also contributes Obsidian primitives such as <code>app</code>, <code>addCommand</code>, <code>addRibbonIcon</code>, <code>addStatusBarItem</code>, <code>registerEvent</code>, and <code>saveSettings</code>. Modules therefore depend on small structural context contracts instead of importing or locating singleton services themselves.</p><h2 id="synthkernel-dependency-injection" tabindex="-1">SynthKernel Dependency Injection <a class="header-anchor" href="#synthkernel-dependency-injection" aria-label="Permalink to “SynthKernel Dependency Injection”">​</a></h2><p>Each module is a class constructor. SynthKernel constructs the internal constructors once, then resolves constructor parameters from the merged context. A module publishes capabilities through properties such as:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">root </span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  executeSync: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.executeSync,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>It can also declare the shape of values it contributes to merged keys:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> readonly </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    syncStarted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isCancelled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: Ref</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">boolean</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">trigger</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string };</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>The declarations make the final <code>Context</code>, <code>Events</code>, <code>Settings</code>, and <code>Translations</code> types derive from the module tuple. They are not a second runtime registry: SynthKernel merges the actual <code>root</code> objects and injects the selected shared keys into module instances.</p><p>The context supports dynamic composition as well. <code>Extensibility</code> verifies and imports an external constructor, calls SynthKernel&#39;s <code>__addModule__</code>, retrieves its instance with <code>__getModule__</code>, and adds the constructor to the plugin&#39;s lifecycle set. A loaded module receives the same merged context as core modules.</p><p>SynthKernel also supplies reactive primitives used across module boundaries. <code>Ref</code> values carry live state such as idle status, cancellation, and progress; <code>computed()</code> derives display state; and <code>hook()</code> owns scoped subscriptions in modal UI. For example, <code>Sync</code> creates one cancellation <code>Ref</code> per run and publishes it through the event bus, while file-system and request layers observe that same reference.</p><h2 id="internal-modules" tabindex="-1">Internal Modules <a class="header-anchor" href="#internal-modules" aria-label="Permalink to “Internal Modules”">​</a></h2><p><code>internalModules</code> is an ordered tuple in <code>packages/plugin/src/index.ts</code>. Order matters for construction, startup, and disposal.</p><ol><li><strong><code>EventBus</code></strong> provides the typed <code>on</code>/<code>dispatch</code> boundary. It owns sync and general logs, tracks idle state, and records task outcomes. Every subscription returns an unsubscribe callback.</li><li><strong><code>I18n</code></strong> selects translations for the current Obsidian language, registers resources from core and external modules, and provides typed string or <code>DocumentFragment</code> translation functions.</li><li><strong><code>Storage</code></strong> owns the Uni-KV memory database and IndexedDB database. It exposes persistent record stores, module metadata storage, and per-local/remote-pair namespaces.</li><li><strong><code>Extensibility</code></strong> discovers module metadata, validates sources and integrity, loads enabled JavaScript modules, persists metadata, and manages enable, disable, update, and unload operations. Its security and trust rules are documented in the <a href="./extensibility">Extensibility Contract</a>.</li><li><strong><code>Registrar</code></strong> is the capability and registry layer. It creates local and remote file systems, applies request middleware and wrappers, selects listers, optimizers, deciders, and conflict resolvers, and exposes registration functions to modules.</li><li><strong><code>Sync</code></strong> executes one sync run: initialize infrastructure, traverse both sides, filter stats, create and transform tasks, request confirmations, execute tasks, and publish lifecycle events. See <a href="./sync">Core Sync Routine</a>.</li><li><strong><code>Observability</code></strong> converts events into user-visible status, progress, notices, commands, ribbon controls, and exported logs. Its reactive values are consumed by the progress modal.</li><li><strong><code>Scheduler</code></strong> turns manual, startup, scheduled, realtime, and vault-change triggers into queued sync requests. It waits for idle state, batches pending requests, and resolves every request in a batch with the same result.</li><li><strong><code>ProgressModal</code></strong> handles progress display, task confirmation, deletion confirmation, cancellation, and failed-task details. SynthKernel <code>computed()</code> values and <code>hook()</code> cleanup keep modal state scoped to the modal lifecycle.</li><li><strong><code>ModulesModal</code></strong> provides the module-management UI. It consumes <code>Extensibility</code> APIs for source refresh, download, enablement, update, and deletion, and cleans up mounted reactive UI on close.</li><li><strong><code>Bootstrap</code></strong> installs the built-in capabilities through <code>Registrar</code>: the default decider and conflict resolvers, request middleware, file-system wrappers, optimizers, remote listers, settings, and English translations. It is last so every registration API it needs already exists.</li></ol><p>The dependency direction is intentionally visible in the constructors. For example, <code>Sync</code> receives <code>initializeSync</code>, <code>listRemote</code>, <code>getDecider</code>, and <code>getConflictResolver</code>; it does not know which backend, wrapper, or middleware supplied them. <code>Bootstrap</code> assembles those policies without changing the sync algorithm.</p><h2 id="extensibility-framework" tabindex="-1">Extensibility Framework <a class="header-anchor" href="#extensibility-framework" aria-label="Permalink to “Extensibility Framework”">​</a></h2><p>External modules extend this same context at runtime. <code>Extensibility</code> verifies and imports an approved constructor, adds it to SynthKernel, merges its <code>moduleSettings</code>, and adds it to <code>allModules</code> so it participates in startup and disposal. Unloading invokes <code>dispose()</code>, removes the constructor, and dispatches <code>moduleUnloaded</code>.</p><p>The module loader is also a security boundary because modules are executable code. Trust, integrity verification, enablement, storage, and runtime privileges are specified in the <a href="./extensibility">Extensibility Contract</a>; module-management UI behavior is covered in <a href="./module-management-panel">Module Management UI</a>.</p><h2 id="registration-pattern" tabindex="-1">Registration Pattern <a class="header-anchor" href="#registration-pattern" aria-label="Permalink to “Registration Pattern”">​</a></h2><p>Most extension points are exposed by <code>Registrar.root</code> as <code>register*</code> functions. The pattern separates registration from consumption:</p><ul><li>Set-backed capabilities use <code>setRegister()</code>. It adds an entry and returns <code>() =&gt; registry.delete(entry)</code>.</li><li>ID-backed capabilities use <code>mapRegister()</code>. It stores an entry under an ID and returns <code>() =&gt; registry.delete(id)</code>.</li><li>CSS registration appends a style element and returns a remover.</li><li><code>registerI18n()</code> follows language selection rules rather than the Registrar registries.</li></ul><p>Examples include <code>registerRemoteFs</code>, <code>registerRemoteFsWrapper</code>, <code>registerRemoteRequestMiddleware</code>, <code>registerRemoteLister</code>, <code>registerDecider</code>, <code>registerConflictResolver</code>, <code>registerSetting</code>, and their local counterparts. A module can therefore add a backend, policy, wrapper, or UI contribution without modifying core code.</p><p>Registration entries are consumed according to their role:</p><ul><li><strong>Factories and ID maps:</strong> the selected remote backend, decider, and conflict resolver are looked up by the ID in settings. Missing IDs fail with an explicit error.</li><li><strong>Wrappers:</strong> request and file-system wrappers are grouped by numeric priority and applied in ascending priority order. Within one priority, the first wrapper that returns a replacement wins; returning <code>undefined</code> declines the current value.</li><li><strong>First-match pipelines:</strong> listers and optimizers are also priority ordered. The first entry that returns a result supplies the implementation for that operation.</li><li><strong>Settings:</strong> setting renderers are ordered by priority and render into the shared plugin settings tab.</li></ul><p>Bootstrap uses these same APIs to build the production pipeline. For example, cancellation, rate limiting, retry, custom headers, memory control, optimization, context caching, and asymmetric storage are independent registrations layered around the base request and file-system implementations. Their behavior is specified in <a href="./request-middleware">Request Middleware</a> and <a href="./file-system-wrappers">File System Wrappers</a>.</p><p>This gives Sync Engine a stable core algorithm with replaceable policy and transport layers. Modules compose capabilities at startup, consume them through typed context APIs, and remove them through the same lifecycle contract when unloaded.</p>`,33)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};