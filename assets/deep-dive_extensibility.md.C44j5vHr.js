import{X as e,Z as t,t as n,ut as r}from"./chunks/framework.es-12sf8.js";var i=JSON.parse(`{"title":"Extensibility Contract","description":"","frontmatter":{},"headers":[],"relativePath":"deep-dive/extensibility.md","filePath":"en/deep-dive/extensibility.md","lastUpdated":1788278373000}`),a={name:`deep-dive/extensibility.md`};function o(n,i,a,o,s,c){return r(),e(`div`,null,[...i[0]||=[t(`<h1 id="extensibility-contract" tabindex="-1">Extensibility Contract <a class="header-anchor" href="#extensibility-contract" aria-label="Permalink to “Extensibility Contract”">​</a></h1><p>Sync Engine loads modules by importing JavaScript files at runtime. A module is executable code, loading a module is <strong>dynamic code execution</strong>. The plugin implements a series of measures to minimize the attack surface.</p><h2 id="trust-model" tabindex="-1">Trust Model <a class="header-anchor" href="#trust-model" aria-label="Permalink to “Trust Model”">​</a></h2><p>The module system trusts:</p><ul><li>the local operating system, Obsidian runtime, vault adapter, memory, and secret storage</li><li>TLS as the transmission layer</li><li>the official module source and its hosted artifacts</li><li>modules explicitly downloaded by the user and their subsequent updates</li><li>sources explicitly added by the user</li></ul><p>The loader guards against:</p><ul><li>module files without existing record to prove user approval are not loaded automatically</li><li>module file must match its recorded SHA-256 value before it is imported, unless explicitly disabled by the user</li><li>compromised custom sources cannot disturb existing modules</li></ul><h2 id="module-sources" tabindex="-1">Module Sources <a class="header-anchor" href="#module-sources" aria-label="Permalink to “Module Sources”">​</a></h2><p>Module source URLs are stored in <code>settings.moduleSources</code>. The default source is <code>https://sync.consensia.cc/modules.json</code>, this is the official source hosted on GitHub pages, fully transparent.</p><p>Sync Engine officially hosts an alternative source <code>https://raw.githubusercontent.com/hesprs/sync-engine/refs/heads/gh-pages/modules-alternative.json</code>. This source is identical to the main source except replaced all <code>sync.consensia.cc</code> to <code>hesprs.github.io/sync-engine</code>. Use this source when your firewall flags <code>sync.consensia.cc</code> as unsafe.</p><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>Please avoid using two sources simultaneously, if you have decided to use <code>https://raw.githubusercontent.com/hesprs/sync-engine/refs/heads/gh-pages/modules-alternative.json</code>, delete <code>https://sync.consensia.cc/modules.json</code> in the sources list.</p></div><p><a href="./../usage/settings#module-sources">Users add and remove source URLs in the setting page</a>. The page accepts <code>http:</code> and <code>https:</code> URLs. Use HTTPS for sources whenever possible.</p><p>Plugin fetches every configured source with Obsidian <code>requestUrl()</code>. A source must contain a JSON array. Each accepted entry must contain string values for:</p><div class="language-TypeScript"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">TypeScript</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ModuleMeta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	id</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	name</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	version</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	description</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	main</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#62687b;--shiki-dark:#818e99;">// Download link</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	icon</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	minPluginVersion</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	readme</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">	integrity</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Optional <code>icon</code>, <code>minPluginVersion</code>, and <code>readme</code> values are also supported. Entries requiring a newer plugin version remain identifiable in the catalog and mark the plugin as outdated; update the plugin before installing those modules. IDs containing common filename separators or metacharacters are skipped, and the integrity string must contain 64 hexadecimal characters.</p><p>Source contents are cached for automatic update checks during the current plugin lifetime. Duplicate IDs are filtered within each source.</p><p>The source entry supplies both the module download URL (<code>main</code>) and the expected integrity value.</p><h2 id="local-module-storage" tabindex="-1">Local Module Storage <a class="header-anchor" href="#local-module-storage" aria-label="Permalink to “Local Module Storage”">​</a></h2><p>Module artifacts are stored in <code>&lt;vault config directory&gt;/plugins/sync-engine/modules/&lt;id&gt;.js</code>. The module directory is inside Obsidian&#39;s plugin configuration directory. New installations explicitly exclude this module directory, as well as the broader vault config directory, from synchronization.</p><p>Installed module metadata is stored in a IndexedDB store whose name is derived from the vault name: <code>modules-&lt;hash of vault name&gt;</code>, so normal attacking method (e.g. compromised syncing backend) cannot arbitrarily modify module meta.</p><p>The stored metadata extends source metadata with:</p><div class="language-ts"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AugmentedModuleMeta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ModuleMeta</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;"> &amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  enabled</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  source</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#c13617;--shiki-dark:#FFAB70;">  icon</span><span style="--shiki-light:#c62739;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>The <code>enabled</code> value used by module loading is stored in this local module record.</p><h2 id="startup-loading" tabindex="-1">Startup Loading <a class="header-anchor" href="#startup-loading" aria-label="Permalink to “Startup Loading”">​</a></h2><p>Startup loading performs the following steps:</p><ol><li>Create the module directory if it does not exist.</li><li>List files and folders below the module directory.</li><li>Remove folders and files that do not end in <code>.js</code>.</li><li>Parse each JavaScript filename into a module ID.</li><li>Match each file with metadata from the local IndexedDB store.</li><li>Open a warning modal for a file with no stored metadata.</li><li>Load a module only when its stored metadata has <code>enabled: true</code>.</li></ol><p>An unknown module is not loaded automatically. The user may configure it through the warning modal or delete it. Configuring an unknown module creates metadata for the local file. Its integrity value is calculated from the file when integrity verification is enabled.</p><p>When loading an installed artifact, the loader reads it through the vault <code>DataAdapter</code>. During a download, it may instead verify the fetched response text directly. When integrity check is not disabled by the user, it calculates SHA-256 over the module text and compares the result with the stored value.</p><p>The module is converted to a JavaScript <code>Blob</code> and dynamically imported only after this check. A default export that is not a function (class constructor) is rejected.</p><p>Integrity verification has these properties:</p><ul><li>It detects modification of a module file after its integrity value was recorded.</li><li>It detects a mismatch between downloaded module text and metadata when an enabled module is loaded immediately.</li><li>It can be disabled for an unknown local module through the module editor. This requires explicit user configuration.</li></ul><h2 id="download-and-update-lifecycle" tabindex="-1">Download and Update Lifecycle <a class="header-anchor" href="#download-and-update-lifecycle" aria-label="Permalink to “Download and Update Lifecycle”">​</a></h2><h3 id="explicit-download" tabindex="-1">Explicit Download <a class="header-anchor" href="#explicit-download" aria-label="Permalink to “Explicit Download”">​</a></h3><p>The Module Management UI exposes a download action for a module that is not installed or has a newer advertised version. The action:</p><ol><li>requests the <code>main</code> URL;</li><li>reads the response as text;</li><li>writes the text to <code>&lt;id&gt;.js</code>;</li><li>stores the supplied metadata in IndexedDB;</li><li>loads the module immediately when it was already running or is marked enabled.</li></ol><p>New modules are disabled by default. The downloaded text is integrity-checked before import when immediate loading occurs. A newly downloaded disabled module is written and stored without being imported; integrity is checked when the module is later enabled.</p><h3 id="install-from-file" tabindex="-1">Install From File <a class="header-anchor" href="#install-from-file" aria-label="Permalink to “Install From File”">​</a></h3><p>The module management menu also offers <strong>Install module from file</strong>. The user picks a <code>.js</code> file (other extensions are rejected with a notice), and the module editor opens with metadata prefilled from the file&#39;s magic bytes, a leading <code>/*! ... */</code> comment of <code>key: value</code> lines embedded at build time. On save, <code>installModule()</code> writes the file to the module directory, stores the confirmed metadata, and loads the module when it is enabled. No source is involved in this path.</p><h3 id="automatic-update" tabindex="-1">Automatic Update <a class="header-anchor" href="#automatic-update" aria-label="Permalink to “Automatic Update”">​</a></h3><p>When automatic update is enabled, the loader schedules one update check after a short delay.</p><p>An advertised update is selected only when:</p><ul><li>its ID matches an installed module;</li><li>its source URL exactly equals the installed record&#39;s source URL;</li><li>its version compares greater than the installed version.</li></ul><p>The update then follows the normal download path. A module that is running or marked enabled is unloaded and loaded from the newly downloaded text. A new module is never enabled by automatic discovery.</p><p>Changing a module&#39;s source through the module editor changes the stored source metadata and is a user action. A different configured source cannot automatically update an existing module because of the exact source comparison.</p><h2 id="enablement-and-unloading" tabindex="-1">Enablement and Unloading <a class="header-anchor" href="#enablement-and-unloading" aria-label="Permalink to “Enablement and Unloading”">​</a></h2><p>The module management UI exposes an enable/disable toggle. Enabling stores <code>enabled: true</code> in the local module metadata and loads the module. Disabling unloads the module and stores <code>enabled: false</code>.</p><p>When loading fails, an enabled module is marked disabled in the local module store and a notice is shown. Loading failures include integrity mismatches, invalid module exports, and import errors.</p><p>Unloading calls the module&#39;s optional <code>dispose()</code> method and removes the constructor from the loaded-module map. Registration cleanup is cooperative: modules are expected to invoke cleanup callbacks returned by registration APIs from <code>dispose()</code>.</p><h2 id="runtime-privileges" tabindex="-1">Runtime Privileges <a class="header-anchor" href="#runtime-privileges" aria-label="Permalink to “Runtime Privileges”">​</a></h2><p>Modules receive the merged Sync Engine Context. The Context includes the live Obsidian <code>app</code> object and module APIs. The plugin also exposes the complete imported Obsidian namespace through <code>syncEngineApiBridge</code>; SDK builds rewrite module imports from <code>obsidian</code> to this bridge.</p><p>The registered module APIs include, among others:</p><ul><li>vault-wide local filesystem access</li><li>remote filesystem creation and wrappers</li><li>request creation and request middleware registration</li><li>settings access</li><li>capability registries for backends, resolvers, optimizers, listers, settings, and CSS</li><li>SynthKernel module management helpers and the shared <code>allModules</code> set</li></ul><p>These privileges are intentional for trusted modules. Similar to how Obsidian gives full API access to user downloaded and enabled plugins.</p>`,53)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};